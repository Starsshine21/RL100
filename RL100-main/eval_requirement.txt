# RL100评估模块依赖
# 用于 eval_rl100.py 和 eval_batch.py

# 核心依赖（应该已从3D-Diffusion-Policy安装）
torch>=1.10.0
numpy>=1.20.0
tqdm>=4.60.0

# 评估专用依赖
tabulate>=0.8.9        # 用于美化表格输出
matplotlib>=3.3.0      # 用于绘制对比图表

# 可选依赖
pandas>=1.3.0        # 如果需要导出CSV
seaborn>=0.11.0      # 如果需要更美观的图表
